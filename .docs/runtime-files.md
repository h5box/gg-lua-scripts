# 目录
- 运行文件
- 历史说明

# 运行文件
以下文件曾由旧站点运行时读写，不提交到仓库：

- `conn.php`：数据库连接配置，公开版本只保留空凭据占位。
- `jb.txt`：上传入口临时写入的 Lua 内容。
- `times.txt`：旧云脚本加载计数。
- `chat_api.txt`、`lua/chat_api.txt`：聊天接口中转文本。
- `send.json`、`lua/send.json`：远程接口返回缓存。
- `.log/`：封禁、IP、聊天等运行日志。

# 历史说明
原域名已不可用；这些文件只用于说明旧运行环境，不表示当前服务可用。
